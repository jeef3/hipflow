'use strict';

describe('Service: documentTitle', function () {

  // load the service's module
  beforeEach(module('slipflowApp'));

  // instantiate service
  var documentTitle;
  beforeEach(inject(function (_documentTitle_) {
    documentTitle = _documentTitle_;
  }));


});
