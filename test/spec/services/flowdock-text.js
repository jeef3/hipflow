'use strict';

describe('Service: FlowdockText', function () {

  // load the service's module
  beforeEach(module('slipflowAApp'));

  // instantiate service
  var FlowdockText;
  beforeEach(inject(function (_FlowdockText_) {
    FlowdockText = _FlowdockText_;
  }));

});
